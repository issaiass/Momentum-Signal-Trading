# Generated export of pyproject.toml's [project.dependencies], for
# environments using plain pip/Docker without `uv sync` or an editable
# install. Keep this in sync with pyproject.toml; don't hand-maintain
# a divergent version list here.
pandas>=2.0
numpy>=1.24
pandas_market_calendars>=4.0
yfinance>=0.2
pandas_datareader>=0.10
ibapi>=9.81
requests>=2.31
pyyaml>=6.0
pydantic>=2.0
matplotlib>=3.7
msal>=1.31.0
